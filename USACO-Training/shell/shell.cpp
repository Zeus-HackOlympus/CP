/*
ID: vishal_7
TASK: shell 
LANG: C++
*/

#include <bits/stdc++.h>

#define FOR(n) for (int i=0; i < n ; i++)
#define REP(I,n,a) for(int i=I; i < n; i+=a) 

using namespace std ; 


int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("shell.in", "r", stdin);
    freopen("shell.out", "w", stdout);
    
    // main code : 
    int n, max, a, b, g; 
    cin >> n ; 
    int arr[n][3] ; 
    for (N) cin >> arr[i][0] >> arr[i][1] >> arr[i][2] ; 
    
    for (int start=1; start <= n; start++) {
        for (int i=0; i < n; i++) {
            int a = arr[i][0] , b = arr[i][1], g = arr[i][2] ; 
            
        }
    }
    return 0 ; 
}
